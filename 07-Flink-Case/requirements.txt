# Fraud Detection Tutorial — Python Dependencies
#
# ✅  Tested on Python 3.11.9, macOS arm64 (Apple Silicon)
#
# INSTALL — run these commands exactly:
#
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install --upgrade pip setuptools
#   pip install apache-flink==2.2.0 --no-deps
#   pip install -r requirements.txt
#
# WHY --no-deps FOR apache-flink:
#   apache-flink pulls in apache-beam, which has no pre-built wheel for
#   macOS arm64 and fails to compile from source on modern setuptools.
#   Our tutorial only uses the DataStream API — apache-beam is never needed.
#
# Java 11 is required to run the PyFlink job:
#   brew install openjdk@11
#   export JAVA_HOME=$(brew --prefix openjdk@11)
#
# ── PyFlink 2.2.0 exact compatible versions ─────────────────────────────────
py4j==0.10.9.7
python-dateutil>=2.8.0,<3
cloudpickle>=2.2.0
numpy>=1.22.4
pandas>=1.3.0,<2.3           # pandas 3.x is NOT compatible with PyFlink 2.2.0
pyarrow>=5.0.0,<21.0.0
pytz>=2018.3
fastavro>=1.1.0,!=1.8.0
requests>=2.26.0
protobuf>=3.19.0
pemja>=0.5.5,<0.5.6          # pemja 0.6.x is NOT compatible with PyFlink 2.2.0
httplib2>=0.19.0
ruamel.yaml>=0.18.4
typing_extensions>=4.0.0
avro>=1.12.0
apache-flink-libraries==2.2.0

# ── Dashboard server ─────────────────────────────────────────────────────────
flask>=3.0.0

# ── Optional: Kafka integration ──────────────────────────────────────────────
# confluent-kafka==2.3.0

